{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ampy",
            "type": "shell",
            "command": "ampy -b 9600 -p COM9 -d 1 run ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}